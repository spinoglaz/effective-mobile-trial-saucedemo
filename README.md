How to run
==========

Setup python 3.12:
```
pyenv install 3.12
pyenv local 3.12
```

Install poetry:

`pip install poetry`

Install project dependencies:

`poetry install`

Run tests:

`poetry run pytest`
